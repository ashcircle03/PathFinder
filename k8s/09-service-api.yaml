apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: pathfinder
  labels:
    app: pathfinder
    component: api
spec:
  type: ClusterIP
  selector:
    app: pathfinder
    component: api
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
